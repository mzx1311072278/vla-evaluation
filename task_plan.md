# Parquet 文件详细分析计划

## 目标

分析 `Genie02_report/zqyh_2cm_mixed_ee_rot6_right_arm_only_eval_pi05_stage2_acp/data/chunk-000/file-000.parquet` 的物理构成、逻辑 schema、每行及嵌套数组的形状，并结合仓库代码解释每个字段和元素的语义、单位与关联关系。

## 阶段

- [x] 阶段 1：确认文件、环境与分析边界
- [x] 阶段 2：提取 Parquet 元数据、schema、行组、编码和样本
- [x] 阶段 3：统计列维度、缺失值、范围、唯一值与时序关系
- [x] 阶段 4：检索字段的生产/读取代码，确认语义、单位和坐标约定
- [x] 阶段 5：形成逐字段、逐元素说明，并标注证据等级和未知项

## 约束

- 只读分析目标 Parquet，不修改源数据。
- 数据统计与代码定义交叉验证；无法由证据确认的含义明确标为推断。
- 控制样本输出规模，不直接转储图像等高维二进制内容。

## 错误记录

| 错误 | 尝试 | 处理 |
|---|---:|---|
| 当前目录不是 Git 仓库 | 1 | 不依赖 Git 状态，继续只读分析 |
| 捆绑 Python 缺少 `pyarrow` | 1 | 检查项目已有 Python/依赖与现成读取代码，不立即安装 |
| 系统 Python 也缺少 `pyarrow` | 1 | 查找项目虚拟环境或其他已安装 Parquet 工具 |
| 沙箱内 pip 无法访问 PyPI | 1 | 经用户授权后下载到 `/tmp`，已成功安装 `pyarrow==21.0.0` |
| 首次分析输出约 335 KB，被终端截断 | 1 | 保留成功读取结果，改为聚合物理列和 episode 元数据后重跑 |
| 将二进制 episode Parquet 误用文本查看 | 1 | 已停止；后续仅用 PyArrow 结构化读取 |
| 环境没有 `ffprobe` | 1 | 视频规格与映射改用 `info.json` 和 episode Parquet 验证 |
